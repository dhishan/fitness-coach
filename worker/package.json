{
  "name": "fitness-mcp-oauth",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --target=es2022 --main-fields=module,main --conditions=workerd,worker,browser,import --external:cloudflare:workers --outfile=dist/worker.js"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.0.5",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.3"
  }
}